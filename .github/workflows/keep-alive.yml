name: "Keep Render Awake"
on:
  schedule:
    - cron: '*/7 * * * *'

jobs:
  ping-render:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Render URL
        run: |
          curl -fsS https://spaced-repetition-tutor.onrender.com/ > /dev/null
